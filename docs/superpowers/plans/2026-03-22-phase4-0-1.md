# Phase 4 Task 4.0-4.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a multimodal observation bridge in `PlatoonEnv` and implement the minimal `RelationEncoder` module required by Phase 4.

**Architecture:** Keep `PlatoonEnv` backward compatible by introducing a config-driven observation mode switch. In multimodal mode, reuse the existing Transfuser feature helpers to emit planner-ready `camera`, `lidar`, `status`, and `formation_relation_state` arrays. Implement `RelationEncoder` as a small MLP with LayerNorm under `models/platoon/`.

**Tech Stack:** Python, PyTorch, MetaDrive, pytest

---

### Task 1: Add acceptance tests for Phase 4 tasks 4.0 and 4.1

**Files:**
- Create: `tests/acceptance/test_phase4_task0.py`
- Create: `tests/acceptance/test_phase4_task1.py`

- [ ] Write failing tests for multimodal platoon observations and `obs_to_tensor()`
- [ ] Run `pytest tests/acceptance/test_phase4_task0.py -v` and confirm failure
- [ ] Write failing tests for `RelationEncoder`
- [ ] Run `pytest tests/acceptance/test_phase4_task1.py -v` and confirm failure

### Task 2: Implement multimodal observation bridge in PlatoonEnv

**Files:**
- Modify: `envs/platoon_env.py`

- [ ] Add `observation_mode` config with default `lidar_state`
- [ ] In multimodal mode, configure MetaDrive observation to `DatasetCollectObservation`
- [ ] Reuse Transfuser feature helpers to build planner-ready `camera`, `lidar`, and `status`
- [ ] Add `obs_to_tensor()` helper with optional device placement
- [ ] Keep lidar-state mode behavior unchanged
- [ ] Run `pytest tests/acceptance/test_phase4_task0.py -v`

### Task 3: Implement platoon relation encoder package

**Files:**
- Create: `models/__init__.py`
- Create: `models/platoon/__init__.py`
- Create: `models/platoon/relation_encoder.py`

- [ ] Implement `RelationEncoder(12, 64, 12)` with LayerNorm
- [ ] Run `pytest tests/acceptance/test_phase4_task1.py -v`

### Task 4: Final verification and logging

**Files:**
- Modify: `EXECUTE_LOG.md`
- Modify: `AGENTS.md`

- [ ] Run both Phase 4 acceptance tests together
- [ ] Update execution log with progress, verification, and any residual risks
- [ ] Update `AGENTS.md` Phase 4 summary if 4.0-4.1 are complete
