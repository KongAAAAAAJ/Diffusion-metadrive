# Single-vehicle diffusion model/runtime parameters shared by train, eval, test, and preprocess scripts.
# target_guidance_type: point | line | multi_point.
# multi_point uses each mode anchor endpoint as that mode's local guidance point.
model_size: small
anchor_method: dynamic  # dynamic | k_means
plan_anchor_path: expert_dataset/anchors.npy
trajectory_reg_decoder_type: gru  # mlp | gru
target_guidance_type: multi_point  # point | # !line删掉 | multi_point
target_line_num_points: 8  # 对应line类型的target_guidance，表示每条线使用多少个点进行表示
preference: true  # false | true；只给分类头加 preference bias，不输入轨迹残差头
preference_bias_beta: 0.2
preference_bias_temperature: 8.0
preference_train_noise_std: 0.05
preference_dropout_prob: 0.02
mode_keep_lane_count: 5
mode_lane_change_left_count: 5
mode_lane_change_right_count: 5
mode_emergency_stop_count: 1

# Future feasible occupancy predictor (disabled by default; set true to enable).
# It predicts an ego-local feasible driving region for classification re-ranking
# only; trajectory generation remains unchanged.
use_occupancy_predictor: false
# occ_grid_h: 64
# occ_grid_w: 64
# occ_sigma_m: 1.0
# occ_lon_range: [-2.0, 30.0]
# occ_lat_range: [-10.0, 10.0]
# occ_residual_scale: 2.0
# occ_adapter_hidden_dim: 64
